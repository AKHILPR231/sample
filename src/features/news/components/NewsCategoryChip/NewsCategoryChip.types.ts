export type NewsCategoryChipProps = {
  /** The event `type` value (e.g. "family_event"). */
  type?: string;
  /** Compact removes the icon and tightens padding (for dense list cards). */
  compact?: boolean;
};
